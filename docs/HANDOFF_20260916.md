# ハンドオフ文書 — 《潜在空間》作曲素材生成ツール（2026-09-16 セッション終了時点）

このファイルは、次のセッションが本セッションの文脈を完全に引き継ぐための記録である。
**次セッションの最初の行動**：本文書末尾の「§12 冒頭で再出力するメッセージ」を、一字一句そのまま最初に出力し、
その後で利用者の指示を待つこと。

- 利用者：goushi@droidrive.com（日本語。長い正式仕様を渡し、自律的な完了を求める。並列に Opus 5 サブエージェントへ委譲してよい）
- リポジトリ：`/Users/goushiyonekura/Claude/latent-space-material`（git、リモート https://github.com/goushiyonekura/latent-space-material 、ブランチ `main`）
- 環境：macOS、システム Python 3.9.6、numpy 2.0.2（`pip3 install --user`）。ffmpeg／brew／sox なし。MP3 のデコードは macOS 標準 `afconvert`。GPU 不要（Modal ワークスペース `ddtalk` は利用者が GPU 用に指定したが未使用）。
- サンドボックスは `~/Downloads` を読めない。利用者は文書をプロジェクトフォルダへコピーして渡す。
- メモリ（セッション横断）：`/Users/goushiyonekura/.claude/projects/-Users-goushiyonekura-Claude-latent-space-material/memory/`（`MEMORY.md` が索引）。

---

## 1. 作品と仕様の要点（変わらない前提）

- 素材（cello 683 s、prism 386 s、scri 710 s、stringquartet 520 s）とゴール（`material_GOAL_001` 30 s）を固定ループで同時開始し、
  **音量だけ**（後に利用者が許可した範囲で再生位置ジャンプと出力ダイナミクスも）で
  `INTRO → OPEN → CONTRACT → GOAL_HOLD → REOPEN → …` の周期を作る。
- 四方式（diffusion / vae / transformer / gan）は「音響配合状態 ξ の理想・関係・可能性の法則」を定義する制御器で、
  ゲインは近似実現の手段。理想への完全一致は不要だが、音量のみ・厳密ゴール・履歴の使用・四方式の異なる計算は必須。
- 配合状態 ξ = [φ̃（対数エネルギー、帯域比率、flux）| 寄与 c（M 本）| 素材ペア関係 upper(R)]。距離 d_ξ² は式(22)。
- ゴール保持区間は `g = e_0` を定数として厳密保持し、生レンダリングが `B·x_0` と一致することを検証する。
- 基準レベル `B = min(1, 0.85 / Σ peak_i)` = 0.214（実素材）。従来版の出力ピークは約 −15 dBFS。
- 状態ラベル：`VALID_APPROXIMATION` / `BEST_EFFORT`（正常出力）/ `HARD_CONSTRAINT_FAILURE` / `INCOMPLETE_IMPLEMENTATION` / `INPUT_ERROR` / `INTERNAL_ERROR`。
  `calibration_status` / `perceptual_status` は常に `UNVERIFIED`。

仕様の原本は利用者が会話で渡した v1.1（H5）。監査側の v1.1 はキー名・式番号が異なる別版で、設定キーの別名変換で吸収している
（`schedule.*`→`form.*`、`motion.relative_rate_max` など。未知キーはエラー）。

## 2. 経緯（時系列）

| 段階 | 内容 | コミット／タグ |
|---|---|---|
| 初期実装（01:36–02:32） | 基盤（Gram 解析、Q5 曲線、Bank、履歴、エンジン）を自分で書き、diffusion/transformer/gan を Opus エージェントへ並列委譲、vae は自作。smoke A〜C 合格、`output/` 生成 | `93f6857` |
| 監査①（改訂 1.1）対応 | ゴール露出方針、運動時間分布、最終目標での再採点＋argmin、設定別名、履歴の時刻順、介入プローブの条件統一、GAN 重要度補正・識別器標準化固定、Transformer 時間尺度、代理／目標／実 PCM の三者距離 | `93f6857` に含む |
| 監査②（コード002 再監査）対応 | sync 不等号修正、counter を局所関係に、**凍結参照に対する実現改善**（局所補正 Bump、確定区間 1.5 s、履歴 ρ(Δt)）、方式別整合（Diffusion 引き寄せ廃止、VAE 音響基底、Transformer 実現済み現在から AR、GAN 基底変更時の再初期化）、OPEN 退化 SOFT 項、長尺 782 s 版 | `b0157f2` |
| 高精細 v1（利用者許可） | 急峻スイッチ、再生位置ジャンプ、マスター段、周期スイープ（採用 180/240/180/240 s）。利用者「めっちゃくちゃ良くなった」 | `463f928`、タグ **`hires-v1`** |
| 断片語彙版（利用者承認） | 理想側を断片語彙の上に置き直し、四方式を並列委譲で改修、24 帯域、ビーム探索、スイープ（採用 120/180/120/240 s） | 本セッション最終コミット |

利用者の直感（「音量変化が遅すぎて原曲が分かる。潜在空間の近似になっていないのでは」）は測定で裏付けられ、律速は実現層ではなく
**理想側**（方式の参照が遅く、連続時計の probe と ξ_G に係留）だった。これが断片語彙版の動機。

## 3. ファイル構成と役割（現状）

```
latent_space/
  audio_io.py      WAV 読み書き（PCM 8/16/24/32, float32/64）
  curves.py        Segment(Q5/HOLD)、Bump（a·64s³(1−s)³ 局所補正）、Clip（位置マップ）、TrackCurve
                   （values/positions/derivatives/state_at、check（解析的）、check_composite（実エピソード、有界拡張）、check_hires）
  form.py          段階計画（整数フレーム）、開放度 o(t)、ユニット（ゴール状態で区切る）
  analysis.py      Gram 事前計算（連続時計）、composition/dist2、composition_from_render、
                   ソロ特徴バンク build_solo_bank、断片語彙 build_fragment_bank、grams_at_positions、
                   composition_from_grams、fragment_composition、random_fragment_composition、fragment_candidates、material_features_at
  bank.py          従来版の合法初期解（Q5 列）、sync（全区間、C2 修正済）／counter（内点区間の局所関係）、変異、時間由来の移動回数上限
  objective.py     E_form（CONTRACT 目標距離、OPEN 非ゴール寄与、N_eff、実エネルギー）、E_hist（段階相対グリッド）、E_motion、J_rel、選択則
  history.py       履歴：観測 observe_committed（ρ(Δt)=1−exp(−Δt/τ_H)、τ_H=60 s）、時刻順イベント、親系譜、mode_state
  engine.py        Job：入力・設定・解析・方式生成、従来版 run_unit（暖機解＋確定区間ループ）、hires 分岐、ハード検査、マスター段、trace
  hires.py         高精細実現器 run_unit_hires（0.5 s 確定、レベル座標探索、位置ジャンプ、断片モードのビーム探索）
  master.py        コンプレッサー → ピーク正規化 → 先読みリミッター
  render.py        式(1) レンダラー（位置マップと継ぎ目クロスフェード対応）
  trace.py         result.wav / gain_curves.csv / state_trace.json
  config.py        DEFAULTS（全確定値）、別名、未知キー拒否、motion.profile（responsive_x2/x4/x8_UNVERIFIED）
  fixtures.py      人工素材
  modes/base.py    方式インターフェース（begin_unit / propose / prepare_reference / window_error / relation_terms / observe_committed / end_unit / signature / trace）
  modes/diffusion.py, vae.py, transformer.py, gan.py
scripts/
  smoke.py         検査 A〜C ＋ 監査対応検査 ＋ hires ＋ 断片モード（全合格が現状）
  report_table.py  完了報告表（output ディレクトリ指定）
  ideal_vs_realized.py  理想がどれだけ音に届いたか（var/hop、速い成分の相関、残差、ゲイン変化量）
  sweep_period.py  周期スイープ（--base= --out= --tag=）
  run_all.sh, prepare_inputs.sh
docs/
  MODE_CONTRACT.md（監査②版の方式契約）、FRAG_CONTRACT.md（断片語彙版の方式契約）、reaudit 指示文書のコピー、本文書
project.json（従来 182 s）、project.explore.json（従来 782 s）、
project.hires.json ＋ project.hires.<mode>.json（v1 採用周期）、project.frag.json ＋ project.frag.<mode>.json（断片版採用周期）
inputs/*.wav（git 管理外、afconvert 済み）、latent_space_code_002.zip（管理外）
```

## 4. 実現層の現状（三つの経路が共存、すべて設定で切替）

1. **従来版**（`hires.enabled=false`）：Bank の暖機解 → 1.5 s ごとに窓参照（10 s）を凍結 → Bump 補正の座標探索 → 確定 → 履歴 ρ(Δt)。
   運動制約（V 0.12/s、A 0.08、J 0.12、相対 dB 12、最小移動 0.02、低速規則）を全面適用。
2. **高精細 v1**（`hires.enabled=true`, `fragment_vocabulary=false`）：0.5 s 確定、0.25 s ランプ（低速規則は利用者許可で放棄）、
   単一トラックの位置ジャンプ（46 ms 特徴で候補選択）、マスター段。
3. **断片語彙版**（`hires.enabled=true`, `fragment_vocabulary=true`）：候補は 2 秒平均の断片特徴、複数トラック同時ジャンプの
   ビーム探索（幅 3、8 行の部分集合で厳密混合評価）、24 帯域（d_ξ=41）、方式内部時間 0.5 s、方式の理想を断片配合の上で生成。

共通：参照は窓ごとに凍結（hash 記録、`reference_updated_during_realization=false`）、改善は音量／位置側のみ、確定ブロックは履歴と
方式の `observe_committed` へ。ゴールは連続時計のまま、CONTRACT 末尾 12 s で Q5 上昇、GOAL_HOLD で厳密 1、REOPEN で Q5 下降。
`mode.update()` は呼ばれない（監査 B5 の禁止事項）。

## 5. 四方式の現状設計（断片語彙版）

- **Diffusion**：配合場 𝓔_D（アンカー項＋ペア項＋三者項＋ridge＋ゴール項、係数 w/s/d/β は確定履歴の h_c, M_H, corr, change_direction から）。
  断片モードでは SDE を音楽時間で積分（model_step 0.5 s、drift サブステップ 10、信頼領域）、アンカーは断片配合 24 本×3 オフセットの平均、
  `frag_step=40`, `frag_temperature_per_openness=0.0016`。window_error = 平均 d_ξ² ＋ κ_E·場エネルギー。relation_terms 供給。
  統計：確定配合の場エネルギー 0.29 vs ランダム断片 0.73。
- **VAE**：基底は断片配合 300×5 行の SVD（W^{1/2} 尺度、k=4 実素材／3 fixture、符号規則、ジョブ内固定、mode_state に保存）、
  デコーダ ξ_G + Σ s_k tanh(z_k) v_k（s_k は 99 パーセンタイル則）、潜在 OU（相関 0.949 s）を model_step の節点で描き行へ補間、
  Σ_H 床 0.09。統計：飽和 0.04%、未使用 30%。
- **Transformer**：断片トークン（素材ごと 6 断片＋ゴール）、値は断片を前景化した厳密配合との差、類似／対照／記憶ヘッド、
  記憶は確定イベント（src_position 付き）＋長期ダイジェスト、AR は model_step で実現済み現在から、放射状有界化（使用率 0.000）、
  dt_scale 0.5（`fragment_step_seconds_reference` = 2×model_step）。統計：注意エントロピー、再帰率。
- **GAN**：参照分布＝断片配合のランダム軌道（12 本、2 s ブロック、ゴール露出方針に従う）、親＝確定配合経路＋新規参照、
  基底 SVD、生成 p_Θ(b,w)、識別器（同一行、標準化は最初のユニットで固定）、D/G 更新は各確定観測で（重要度補正・ESS）、
  基底変更時に μ/log σ 再初期化。統計：D 確定 0.27 vs ランダム断片 0.54。

各方式のローカル設定キーはすべて `config.py` の DEFAULTS に登録済み（未知キーは拒否されるため）。

## 6. 出力と数値（すべて BEST_EFFORT、ハード検査合格、ゴール完全一致）

| ディレクトリ | 設定 | 内容 |
|---|---|---|
| `output/<mode>/` | project.json | 従来版 182 s。方式あたり 80〜120 s で生成 |
| `output_explore/<mode>/` | project.explore.json | 従来版 782 s（素材が 1.1〜2.0 周）。約 20 分／方式 |
| `output_hires/<mode>/` | project.hires.<mode>.json | 高精細 v1。採用周期 diffusion 180 / vae 240 / transformer 180 / gan 240 s。バックアップ `output_hires_v1_backup/` |
| `output_frag/<mode>/` | project.frag.<mode>.json | 断片語彙版。採用周期 diffusion 120 / vae 180 / transformer 120 / gan 240 s（全長 322/442/322/562 s） |

WAV（`result.wav`）はすべて git 管理外（GitHub の 100 MB 制限）。trace の曲線・位置マップ・マスター設定から再レンダリング可能。

断片語彙版の残差（全グリッド固定参照、24 帯域空間、3 区間）：diffusion 0.21/0.20/0.15、vae 0.27/0.25/0.19、
transformer 0.25/0.20/0.15、gan 0.61/0.41/0.20。同設定の改修前方式と比べ fixture で diffusion 0.46→0.12、transformer −45〜−54%、gan 0.40→0.27〜0.30。
`ideal_vs_realized.py`：理想の 0.1 s あたり変化 0.04〜0.27、実現 0.14〜0.18、速い成分の相関 0.05〜0.14（素材自身の内部変化が支配的）。
マスター段：正規化 +12.8〜+17.4 dB、コンプレッサー最大 −17 dB、リミッターほぼ非作動、出力ピーク 0.891。

## 7. 正直な留保（次セッションで誤解しないために）

- 「方式の潜在空間に近づいた」は、固定参照残差と 0.5 s 単位の統計（場エネルギー、D 値、有界化使用率、tanh 使用率）で示している。
  0.1 s 単位の速い成分の相関は依然低く、素材の内部変化が支配する。
- 高精細 v1 が「めっちゃくちゃ良く」なった主因は、断片化（ジャンプ）・急峻スイッチ・マスター段・長い OPEN であり、方式の近似精度ではない
  （v1 の残差は従来版より悪化していた）。断片語彙版はその上で理想側を到達可能集合に合わせたもの。
- 周期スイープの差は数％で優位は弱い。GAN の第 1 区間は残差が大きい（参照が散る）。
- 運動上限・聴感の校正は未実施。高精細版は低速規則を放棄（利用者許可、trace に `waived` と記録）。
- 断片は原素材の音そのもので、素材にない音色は出ない。

## 8. 実行方法（コピペ用）

```bash
cd /Users/goushiyonekura/Claude/latent-space-material
python3 scripts/smoke.py                                                    # 全検査（約 6 分）
python3 -m latent_space generate --config project.frag.diffusion.json --mode diffusion --output output_frag/diffusion
python3 scripts/sweep_period.py --base=project.frag.json --out=output_frag --tag=frag        # 周期スイープ（方式名を引数に、並列可）
python3 scripts/report_table.py output_frag
python3 scripts/ideal_vs_realized.py output_frag output_hires
afplay output_frag/diffusion/result.wav                                     # 試聴
```
長時間ジョブはバックグラウンドで起動し、ログ（`dev/sweep_*/<mode>.log`）を待つループで監視した。
元に戻す：設定を `project.hires.<mode>.json`（v1）や `project.json`（従来）に替えるだけ。コードごと戻すなら `git checkout hires-v1`。

## 9. 次の候補作業（利用者の判断待ち）

1. 断片語彙版の試聴結果を受けた調整（min_clip、ジャンプ候補数、レベル刻み、周期、マスター設定）。
2. 理想の速い成分をさらに出すなら：方式の model_step を 0.25 s へ、確定 0.25 s、ランプ 0.1 s（コスト約 2 倍）。
3. 素材ごとのラウドネス正規化（基準ゲイン b_i を素材別に、ピーク上界は維持）。
4. 素材数の増加（N=8〜12）で到達可能集合を広げる。
5. `responsive_x2/x4` プロファイル（従来版の運動上限を時間短縮則で緩める）の比較出力は未生成。
6. GAN 第 1 区間の参照散乱の抑制（参照を確定履歴に近い断片へ絞る）。

## 10. 委譲の作法（このセッションで有効だった方法）

- 共有基盤とインターフェースを先に固め、契約文書（`docs/MODE_CONTRACT.md`、`docs/FRAG_CONTRACT.md`）を書いてから、
  方式ごとに Opus 5 サブエージェントへ並列委譲（各自の方式ファイルのみ編集、共有変更は報告させる）。
- 各エージェントには検証コマンドと報告項目を明示し、fixture（`dev/fixture_vae.json` 従来、`dev/fixture_hires.json`、`dev/fixture_frag.json`）で
  90 秒以内の実行を要求した。
- 委譲中は自分で smoke／実素材実行／診断スクリプトを進め、完了通知後にローカル設定キーを DEFAULTS へ登録した。

## 11. git

- `main`：`93f6857`（初期＋監査①）→ `b0157f2`（監査②）→ `463f928`（高精細 v1、タグ `hires-v1`）→ 本セッション最終コミット（断片語彙版）。
- `.gitignore`：mp3、inputs/、dev/、各 output*/…/result.wav、output_hires_v1_backup/、latent_space_code_002.zip。

## 12. 冒頭で再出力するメッセージ（一字一句そのまま。次セッションはまずこれを出力する）

```
断片語彙版の実装・四方式の改修・周期スイープ・検査まで完了しました。現状は未コミットです（元に戻す可能性を考慮）。

## 保持した状態

- 改修前は git タグ `hires-v1`（push 済み）で固定。高精細 v1 の出力は `output_hires_v1_backup/` にコピー済み。
- 新方式は opt-in（`hires.fragment_vocabulary: true`）で、従来版・高精細 v1 は設定を変えなければ動作が変わりません（従来設定の出力は変更前ファイルとビット一致することを各方式で確認済み）。

## 実装したもの

- **共通基盤**：断片語彙（各位置から 2 秒ぶんの平均特徴）、断片配合の厳密計算（その位置の実 PCM の Gram）、複数トラック同時ジャンプのビーム探索、24 帯域（d_ξ=41）、方式内部時間 0.5 秒。
- **Diffusion**：場の SDE を音楽時間で積分（0.5 秒ごと）、アンカーは断片配合 24 本の標本平均。
- **VAE**：断片配合 1500 行の SVD 基底（k=4）、tanh の有効域を使う尺度。
- **Transformer**：断片トークン（各素材 6 断片）への注意、値は断片を前景化した厳密配合。
- **GAN**：参照分布＝断片配合のランダム軌道、識別器はそれと確定配合を判別。

## 結果（`output_frag/<mode>/`、周期は残差最小で採用）

| 方式 | 採用周期 | 全長 | 全グリッド固定参照残差（3区間） | 方式固有の統計 |
|---|---|---|---|---|
| diffusion | 120 s | 322 s | 0.21 / 0.20 / 0.15 | 確定配合の場エネルギー 0.29 vs 断片ランダム 0.73（比 0.39）、ドリフト／ノイズ ≈ 1 |
| vae | 180 s | 442 s | 0.27 / 0.25 / 0.19 | tanh 飽和 0.04%、未使用 30%（v1 は飽和 13%） |
| transformer | 120 s | 322 s | 0.25 / 0.20 / 0.15 | 有界化の使用率 0.000（v1 は 1.00）、注意エントロピー 類似 1.38／対照 0.66／記憶 1.78 nats |
| gan | 240 s | 562 s | 0.61 / 0.41 / 0.20 | 識別器 D：確定配合 0.27 vs ランダム断片 0.54（分離している） |

同じ設定で改修前の方式と比べた fixture 上の残差は、Diffusion 0.46→0.12、Transformer −45〜−54%、GAN 0.40→0.27〜0.30 と大きく下がりました。VAE は k 因子の打切り床（0.79）近くまで下がっており、それ以上は因子数の制約です。ゴール到達（132 s／287 s など）、生レンダリングでのゴール一致、ハード検査はすべて合格、smoke も全項目合格です。

## 正直な留保

- 理想が動く量は「0.5 秒ごとに実現可能な幅」（0.15〜0.43 d_ξ）に合わせて調整しましたが、0.1 秒刻みで見ると実現配合の変動は依然として素材自体の内部変化が支配的で、理想と実現の速い成分の相関は 0.05〜0.14 にとどまります。方式差は「0.5 秒単位の断片選択とレベル」のスケールに現れます。
- GAN の第 1 区間は残差 0.61 と大きく、参照分布がランダム断片軌道なので序盤は目標自体が散っています。
- 周期の差はいずれも数％で、採用周期の優位は弱いです。

## 聴く場所と戻し方

```
/Users/goushiyonekura/Claude/latent-space-material/output_frag/diffusion/result.wav   322秒
/Users/goushiyonekura/Claude/latent-space-material/output_frag/vae/result.wav         442秒
/Users/goushiyonekura/Claude/latent-space-material/output_frag/transformer/result.wav 322秒
/Users/goushiyonekura/Claude/latent-space-material/output_frag/gan/result.wav         562秒
```

高精細 v1 は `output_hires/`（バックアップ `output_hires_v1_backup/`）にそのまま残っています。
```

（注：上のメッセージ中の「現状は未コミットです」は、そのメッセージを書いた時点の状態。本ハンドオフ作成時にすべて commit／push 済み。
再出力後に、その旨を一行添えること。）
