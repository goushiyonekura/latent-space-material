# 《潜在空間》コード002再監査・Claude Code修正指示【統合版】

- 日付：2026-09-16
- 直接監査したコード：`latent_space_code_002.zip`
- ZIP SHA-256：`89902035268e3cb6a0d2aea2847f267da642d98cb1cf4cf4d46c203694984640`
- 照合した過去のGitHubスナップショット：`93f68575f0064761b6bd368a48e0ce402878dc18`
- 出力：前回提供された `output_002.zip` の4方式のWAV・CSV・trace。
- 用途：今回の追加監査と、既存リポジトリを直接修正するための指示。GUI、新規サービス、大規模学習、別の検証基盤を作る依頼ではない。
- ソースの行番号：**今回のZIPを展開した直後の物理行番号**。修正後は関数名で追う。
- この監査で製品コードを修正したわけではない。実施したのは静的確認、既存出力・traceの再照合、限定的な関数実行、および監査用の小さい構成例の計算。

> **最重要：四方式が定義するのは音響配合の目標・関係・可能性の法則。ゲインは、その法則を現在の素材音響で近似実現する唯一の操作変数である。目標を出力候補へ寄せるだけで残差を下げるのではなく、固定した目標に対して、合法な音量軌道の側を改善する。**

---

## A. 再監査の結論と証拠範囲

### A1. 今回はソースを直接実行できた

ZIP内の主要25ファイルのGit blob hashが、前回取得した上記コミットの対応ファイルと一致した。これには `latent_space/` のPythonコア、`project.json`、README、`docs/MODE_CONTRACT.md` が含まれる。比較できた25件は25件とも一致。ZIP全体をGitコミットと同一のアーカイブだと主張するものではない。

したがって前回の監査は、別の古い実装を見ていたわけではない。今回は実ファイルから関数を呼び、前回の推論の一部を実行で確認し、**同方向連動の追加不具合**などを発見した。

原素材5本は今回のコードZIPにもない。元PCMを使う4方式の全生成は再実行していない。前回の実WAV測定値は `output_002` の結果として継承し、今回の人工例の結果と混ぜない。

### A2. 結論を四段階に分ける

| 観点 | 判定 |
|---|---|
| 実PCMを分析しているか | している。Gramによる実混合評価、交差項、4方式の音響目標がある |
| 音量のみ・滑らかさ・ゴール保持 | `output_002` の曲線・出力に対する前回の良好な検査結果を維持。原素材との全再合成は未独立確認 |
| 方式固有の内部機構 | 存在する。ただし音響目標の自己適応、狭い基底、固定参照、粗い確定更新などが表現を限定している |
| 構想が出音へ十分に現れたか | 現状では十分とは判断しない。合法性の合格だけで「これが潜在空間の必然的な音」と説明してはいけない |

短い、遅い、単純という印象の説明は一つではない。**形式・運動制約・候補族・関係を候補へ変える不具合・履歴の更新単位・目標の移動**が重なっている。

### A3. 旧指摘を修正済みとして扱うもの

以下を最初から作り直さない。

- INTRO/OPENでのゴール露出抑制。今回の出力では0が保たれている。
- 最終の固定目標集合での再採点と、argmin／許容差内選択。
- 設定aliasと未知キーのエラー処理。
- 履歴イベントを時刻で並べてから最近のものを保持する処理。
- pre-unit状態とRNGをそろえる履歴比較。
- GANの標本再利用に対するimportance補正・ESS停止、および識別器入力標準化の固定。
- 実レンダリング音と、代理評価・採用目標を同じ局所窓で比べる処理。

importance補正は有限標本の近似であり厳密不偏性を主張しないが、「無補正のまま」という前回初期版の指摘を繰り返さない。Transformerの放射状tanhも方向を保つ安定化であり、出力PCMのクリッピングではない。

---

## B. 問題の詳細：前回の結果と今回の追加確認

### B1. 182秒・ゴール62秒は、モデルが発見した時間ではない

**所在：`project.json:16–24`, `engine.py:130–143`, `form.py:94–150`。**

今回の形式は、INTRO 12秒、OPEN 20秒、CONTRACT 30秒、GOAL_HOLD 5秒、REOPEN 30秒、2周期＋最後のREOPEN。合計182秒。最初の完全ゴールは62秒、次は147秒。

`Job.setup()` は `FormPlan.build()` を先に実行し、それから音響解析器・方式制御器を作る。形式の長さは音響モデルが収束診断から決めるわけではない。`adaptive` は主に時間の実行可能性調整であって、自律的な音楽形式生成ではない。

OPEN合計40秒、全体の約22%。収縮・再可能化・保持が130秒、約71%。短い展開の直後に長いゴール接近が始まるため、ゴールがすぐ現れるのは設定から説明できる。

非ゴール素材の長さは、traceによると約683.367、385.707、709.500、519.713秒。**182秒では一本も全長ループしていない。** 内部音響は変化するが、独立周回による重なり方の変化はまだ利用されていない。

これはループ演算のバグではなく、試験の長さと作品の狙いの不一致。固定形式自体も誤りではない。自律的な収束開始を新たな必須機能にする必要はないが、設定由来の時間をモデル固有の必然と説明してはならない。

### B2. 音量運動が遅い主因は、曲線族と時間配分

**所在：`config.py:25–35,64–80`, `bank.py:112–269`, `curves.py:119–191`。**

`output_002` の非ゴールの単調移動の中央値は、Diffusion 8.013秒、VAE 8.225秒、Transformer 9.337秒、GAN 8.319秒。振幅中央値は約20〜29 points。これは「0.1秒ごとに音量が独立に再決定される」制御ではない。

現在のQ5は

\[
Q(s)=10s^3-15s^4+6s^5,\quad g=a+(b-a)Q(s)
\]

であり、\(\Delta=|b-a|\) とすると

\[
V_{peak}=\frac{1.875\Delta}{T},\quad
A_{peak}=\frac{10\sqrt3\Delta}{3T^2},\quad
J_{peak}=\frac{60\Delta}{T^3}.
\]

既存の \(V_{max}=0.12,A_{max}=0.08,J_{max}=0.12\) から、0→100のQ5には速度だけで15.625秒以上、5 pointsでもジャークだけで約2.924秒以上かかる。

**これは任意の連続制御に対する普遍的な下限ではない。各移動を停止から停止までのQ5にしたことによる下限である。** 途中で止まらず動く曲線の中に短い判断区間を置くことは、これとは別。

新Bankでは小・中・大の振幅分布を実装している。一方、`_segment_plan()` は残り時間を一回の合法な終端ランプで埋められると即座にそれを採用する。終盤が20〜30秒級の単調フェードになりやすい。

`max_moves_per_track=24` は安全上限と記載されているが、OPENを大きく延ばした場合には実際の上限として効く。**出力時間だけ延ばしても、24回に詰め込むため長い動きや保持が増える可能性がある。** 長尺化と局所制御化は一緒に整理する。

### B3. NEW/P0：同方向 `sync` にも不等号の誤りがある

**所在：`bank.py:424–434`、特に432行。**

最後がゴール0へ下降するなら、最後から一つ前の値 \(x\) に必要なのは

\[
x\ge g_G+\Delta_{min}=0.02
\]

である。しかし現在のコードは、その前の上昇 \(x=level+\delta\) に対し

```python
dhi = min(dhi, (vG + self.dmin - level) * d)
```

を適用する。\(d=+1\) では実質的に **\(x\le0.02\)** へ狭めており、必要な下限を上限にしている。

今回、製品コードの関数をそのまま呼んだ最小例：

- 基準曲線：0→0.5を10秒、0.5→0を10秒、その後2秒のゴール保持。
- 既存 `TrackCurve.check()`：違反なし。
- 最初の10秒に合法な振幅としてコードが返す区間：約0.06983〜0.63458。
- 誤った追加条件：上限0.02。
- 結果：`derived_track_plan(..., 'sync')` は5つのseedすべてで `None`。
- 監査用のメモリ内関数だけで不等号を正しく変換すると、同じ5条件で5件とも合法な同期曲線が返った。

**これは単なる表現上の好みではなく、具体的な実装不具合。** 全音響の同時変化がなくなるわけではないが、方式が指定する関係を候補へ伝える機構を不必要に拒否している。

証拠：`focused_check_results.json`, `repair_feasibility_demo.json`。修正例は製品へ適用しておらず、限定した構成確認のみ。

### B4. counterは、開始だけでなく終点にも全区間逆向きの矛盾がある

**所在：`bank.py:377–402`。**

非ゴールが両方0から開始するなら、最初の非ゼロ移動は両方とも上昇。片方に全区間反転を要求すると、もう片方に0からの下降を要求して拒否される。

両方が0へ戻る終端でも、最後の非ゼロ移動は両方とも下降となるので、全区間を完全逆向きにする条件は成立しない。

今回も実関数の最小例でcounterが拒否されることを確認した。ただし独立曲線が途中で逆方向に動く可能性はあり、出力に反相関が皆無とは言わない。

**修正対象は局所関係。** 立ち上がり・終端から切り離した内点区間で、同方向／逆方向／時間差の関係を実現する。素材PCMの開始位置をずらすことではない。

### B5. 理想側への50%引き寄せを、実音側の改善と分離できていない

**所在：`modes/diffusion.py:470–492`, `modes/vae.py:111–121`, `engine.py:219–296`。**

Diffusionには

\[
X^*_{new}=X^*_{old}+0.5(A_{candidate}-X^*_{old})
\]

がある。実音Aを固定したままでも、二乗残差は1回で1/4。5回なら1/1024になる。VAEでも実現候補の推定潜在位置へ中心を半分寄せる。

前回の実出力ログでは、Diffusionの初回の最良target-fitが0.402415→0.001849へ低下したが、候補側の平均場エネルギーは0.691587→0.688003へ少し下がった後、同じ値を繰り返した。

**現状は「目標と実現を共同で変える探索」。それを「固定した目標を実音でよく近似できた」とは説明できない。** 元仕様の `mode.update()` 契約自体がこの実装を誘導しており、CCだけの誤読と扱わない。今回、この契約を明示的に改める。

理想の自己適応をすべて禁止する必要はない。既知の素材から到達可能性を見積もって目標を作ること、確定結果を次の計画へ反映することは残す。禁止するのは、**同じ評価の途中で参照目標を移し、その減少だけを音の改善として計上すること**。

### B6. 配合目標が速くても、遅い軌道への平均距離では細部が消えやすい

**所在：`types.py:110–115`, `modes/base.py:19–29`, `modes/vae.py:92–101`。**

理想は0.1秒尺度でサンプルされ、VAEのAR係数0.9は \(-0.1/\log0.9\approx0.949\) 秒の相関時間に対応する。一方、実ゲインの移動中央値は約8秒。

説明用に、実現側が区間中ほぼ一定aしか出せず、目標が \(X_l\) の列なら

\[
\frac1L\sum_l\|a-X_l\|^2
=\|a-\bar X\|^2+\frac1L\sum_l\|X_l-\bar X\|^2.
\]

右の第2項はaに依存しない。したがって高速の目標変動を追えない場合、平均を近似することが最適になる。実コードは完全な定数ではないが、**「内部が細かいから出音も細かい」とならない仕組み**をこの式が説明する。

モデル時間、解析hop、実現軌道の応答時間を別々に定義する。速い乱数を増やすだけで改善したとしない。

### B7. NEW/P1：GANの生成側の潜在座標が、基底変更時に継承されていない

**所在：`modes/gan.py:260–294` と `331–370`。**

識別器入力の標準化固定は修正済み。一方、生成側は毎unit `B_ref` を新しい参照配合のSVDで作り直すが、親IDが一致すると旧 `mu`, `log_sigma` をそのままコピーする。

\[
A^*=A_{parent}+B_r w
\]

において \(B_{r+1}\ne B_r\) なら、同じ数値wが同じ音響方向を表すとは限らない。次元が同じでも解決しない。SVDの符号だけが反転する例でも問題になる。

今回の構成例では、旧基底 \(B=[1,0]^T\)、新基底 \(-B\)、実際の `_init_theta()` が継承する平均0.3に対し、平均音響変位は `[0.3,0]` から `[-0.3,0]` へ反転した。これは**座標継承の反例**であって、実素材の基底が実際にその符号反転を起こしたという測定ではない。

最小修正は、音響の親履歴は保持し、新基底の変位係数だけを平均0・明示した初期分散で再初期化すること。親・識別器・共通履歴を消すのではない。より正確な座標輸送は後述するが、今回それを巨大な実装にする必要はない。

### B8. NEW/P1：OPENの最低条件は「非ゴールが何かある」程度

**所在：`objective.py:47–60`。**

現在のOPEN評価は、非ゴール寄与の総和が0.25以上かを見る。ゴール0で非ゴールが一つ十分に存在すれば、その総和はほぼ1。

実関数による確認：`c=[0,1,0]` と `c=[0,0.5,0.5]` はどちらもOPENペナルティ0。完全無音でもこの項の最大は0.0625、既定の重み0.5を掛けると0.03125。

これは全目的関数が二つを区別しないという意味ではない。mode距離は区別し得る。しかし、**共通形式側には複数素材が共存する目標も、実混合の低エネルギーを抑える項もない。** 初回のランダムZERO_HOLDが重なることも許されている。前回のTransformer出力の冒頭約10.4秒が完全ゼロだった問題と整合する。

強制的に全素材を常時鳴らす修正はしない。複数素材の関与・実エネルギー・長い無音をSOFT評価として加え、意図的な単一音響への収縮と区別する。

### B9. 履歴は実在するが、確定結果を返すのは3回

**所在：`engine.py:190–345,392–410`, `history.py:49–116`。**

今回の確定単位は67秒、85秒、30秒。unit内の理想自己回帰や候補の内部更新はあるが、実現して採用する配合を共通履歴へ確定反映するのはunit終了後。

前回の同条件履歴対照は8件中5件で同じ出力候補を選んだ。これは固定Bank・共通履歴コスト除外という局所対照であり、履歴の総効果が0だと断言しない。それでも「理想の差が有限Bankへの近似で消える」例があることは重要。

オフラインで構わないので、長い作品形式の内側に短い**確定区間**を設け、実際に採用した配合を次の計画へ返す必要がある。解析点を増やすだけ、ログだけを小分けにするだけでは同じ。

### B10. 各方式の表現の限界を、実装バグと混ぜない

- Diffusion：場はある。固定probeへ寄せるanchor項の値が初回の実現場エネルギーの約75.7%だった。ただし**値の内訳が75.7%ということは、候補の順位・勾配への影響も75.7%だという証明ではない**。anchorを単にゼロへ落とすより、項別の値と候補改善を分ける。
- VAE：2因子は存在するが、`[[1,3],[2,4]]` というファイル順のグループで骨格を作る。これは実音から学習した一般的な潜在構造ではない。初期基底を音響由来にする方が狙いに合う。
- Transformer：注意が働き、放射状有界化も妥当。高い飽和率だけで注意が無効とは言わない。dt補正は追加済みだが、AR相関係数と有界化を適用する頻度も時間尺度に関係するので、解析hopを変えるだけで表現を変えない設計にする。
- GAN：親重みがほぼ一様でも、それ自体は不具合ではない。多様性を無理に失わせない。同じBankの参照と候補を使うことも敵対的制御として成立する。ただしBankの語彙が狭ければ、そこでの学習だけで新しい時間構造を発明できる保証はない。

### B11. NEW：開発指示書に、今回の修正を妨げる古い規則が残る

**所在：`docs/MODE_CONTRACT.md:37–57,78–86`, `modes/base.py:1–7,61–65`。**

同梱の契約には、`engine.py/bank.py/analysis.py/history.py/vae.py` 等を編集しない、問題はmode内で回避するという並列開発時の制限が残る。また選択則の説明が旧softmaxのままで、modeは実現候補に応じて内部更新することを強く要求している。

今回の主問題はまさに共有エンジンと実現層にある。**この古い作業制限を有効な正本として読むと、目標側の回避だけが増える。** 最初に更新する。

### B12. 局所再計画で使ってはいけない既存のAPI

**所在：`curves.py:301–306`。**

`velocity_after(frame)` は実際には速度ではなく `seg.direction` の ±1 / 0 を返す。現コア内に呼び出しは見つからず、現在のWAVがこれで壊れたという指摘ではない。

しかし局所再計画の境界速度取得に流用してはならない。今回、10秒で0→0.5のQ5の中点で、この関数は1、正しい速度は0.09375 gain/sだった。

`derivative_at(frame, order, fs)` または `state_at(frame,fs) -> (gain,velocity,acceleration)` を正しい単位で実装する。

---

## C. Claude Codeへの正式な修正指示

以下を今回の作業指示とする。要件v1.1の **音量のみ・固定時計・滑らかな順次変化・ゴール・4原理・履歴** は継承する。この指示と衝突する旧 `MODE_CONTRACT.md` の編集禁止・目標引き寄せ前提・旧選択説明は撤回する。

### C0. スコープと優先順位

**目的は監査を再現することではなく、音響配合の差が出音へ届くように実現層を直すこと。**

- 既存リポジトリで作業する。監査用のディレクトリ配置を再現しない。
- GUI、Web、APIサーバー、DB、Docker、CI、プラグイン、大規模モデル学習を作らない。
- 原素材が作業環境にあるならそれを使う。なければ既存fixtureで実装し、原素材での聴覚検証を未実施とする。
- 元の工数制約を引き継ぐ。許可された修正枠内で、上から順に実働化する。無断の期限延長、別検証プロジェクトへの移行は禁止。
- 速さのために4方式を同じクロスフェードへ置換しない。時間不足時は未達を明記し、核心が未実装なのに「完成」としない。
- 以下には、確定したバグ修正と、新しい表現設計の要求がある。後者を「旧コードが最初から仕様違反だった」と説明しない。

作業順：**(1)契約更新・sync/counter修正 → (2)固定目標と音響誤差駆動の実現 → (3)短い確定区間と履歴 → (4)方式別の整合 → (5)時間・速度の比較出力。**

(1)だけで終了した場合は `CORE_BUGFIX_ONLY` と報告する。「音響実現層の問題まで解決」とはしない。

### C1. 絶対に維持する入出力条件

\[
y[n,c]=\sum_{i=0}^{N}b_i g_i(n/f_s)x_i[n\bmod L_i,c].
\]

- ゴール添字0。Nは実行時可変。全音源の時計はサンプル0から進行。
- ゲイン0でも再生位置を進める。音声の開始オフセット、速度、ピッチ、全長ループ、帯域、パン、残響等を変更しない。
- 入力PCMと静的基準値bは計画中不変。後段のリミッター・ノーマライズで帳尻を合わせない。
- 全ゲインは0〜1、連続な値・速度・加速度、明示した速度・加速度・ジャーク・相対速度の範囲内。
- ゴール保持では厳密に \(g=e_0,\dot g=\ddot g=0\)。直前のスナップは禁止。
- INTRO/OPENのゴール0、REOPENは1を継承する現在の露出方針を維持。
- 同じゴールに戻っても、モデル履歴・素材時計をリセットしない。
- 近似誤差が残ることは正常。全ハード制約が合法なら有限探索の最良解を返す。ただし計算の未実装を近似とは呼ばない。

### C2. sync不等号の最小修正

振幅δの範囲をいきなり上下どちらかへ制約せず、先に「次のゲインxが入る範囲」を作り、方向dに応じてδへ変換する。

```python
next_lo, next_hi = 0.0, 1.0
if final_direction > 0:  # 次の最終移動がゴールへ上昇
    next_hi = min(next_hi, goal_gain - minimum_move_amplitude)
else:                   # 次の最終移動がゴールへ下降
    next_lo = max(next_lo, goal_gain + minimum_move_amplitude)

if direction > 0:
    delta_lo = max(delta_lo, next_lo - current_gain)
    delta_hi = min(delta_hi, next_hi - current_gain)
else:
    delta_lo = max(delta_lo, current_gain - next_hi)
    delta_hi = min(delta_hi, current_gain - next_lo)

if delta_lo > delta_hi:
    return None
```

その後も、実際に選んだ値と残り時間について最終ランプの合法性を確認する。この交差だけで全時刻の合法性が自動保証されるとはしない。

確認はB3の最小例で足りる。基準曲線そのものは合法なのに、必ず派生が不成立になる状態を解消する。

### C3. counterを局所配合関係へ変更する

- 開始0と終端0では逆向き制約を要求しない。
- 両素材が内点にある将来区間を選び、その区間だけ上昇／下降が反対になる候補を作る。
- 同期は同時・同方向を常に強制する意味ではなく、モデルの音響関係を実現する補助ヒント。候補は必ず全体の実混合で評価する。
- ゲイン速度の符号が逆でも、元素材の内部音量が変われば実寄与は同方向になる場合がある。したがって最終の関係評価は寄与cや音響状態の変化へ置く。ゲインの関係は操作案にすぎない。

最小のSOFT関係項の例：

\[
J_{rel}=\left\langle\sum_{i<j}\omega_{ij}
(\Delta_\ell c_i-s_{ij}\Delta_\ell c_j-d^*_{ij})^2\right\rangle_{I_{interior}}.
\]

\(s=+1\) は同方向、\(s=-1\) は反対方向。時間差 \(\ell\) の意味を秒で定義する。重み・目標は方式から供給し、全方式共通の装飾的揺らぎにしない。ゲインの和を1に固定しない。

`attempted/applied/infeasible_boundary` の小さい集計だけ残す。失敗時に黙って独立曲線へ戻ったことが隠れないようにする。関係項が強制的に実現できなくても、合法近似は出す。

### C4. モデル更新と実現最適化を分離する

計画時刻kで

\[
\mathcal T_k=(P_k^*,\{A_{k,a}^*\},\{\pi_{k,a}\},\Theta_k,W_k,F_k)
\]

を作り、**実現候補の改善中はこの評価スナップショットを不変にする**。乱数列、音響スケール、場の係数、参照目標、選択規則を含む。

区別すべき処理：

1. `prepare_reference(context, history)`：素材・確定履歴・方式固有の内部運動から目標を作る。
2. `realize_frozen_reference(reference, boundary, warm_tail)`：目標を変えずにゲイン軌道を改善する。
3. `commit_prefix(...)`：採用した音響経路の先頭を確定する。
4. `observe_committed(...)`：確定した部分だけを実履歴へ入れ、次の参照を作る。

Diffusionの場内探索は(1)で残す。VAEの潜在分布生成、Transformerの注意・仮説、GANのD/G交互更新も残す。ただし、内部学習／探索と固定参照への実現改善を同じ数値で説明しない。

初版修正ではDiffusionの `realized_pull` とVAEの候補追従 `z_shift` を、**固定参照を追う内側ループでは無効**にする。履歴から次の参照を作る更新まで消さない。GANの交互更新を消す意味でもない。

記録するのは、最小限次の値：

```text
reference_id / reference_hash
reference_source_history_version
initial_fixed_target_error
final_fixed_target_error
initial_joint_objective / final_joint_objective
reference_updated_during_realization = false
mode_term_breakdown
```

将来目標適応を再導入するなら、旧目標・新目標への残差を分ける。到達しやすさを見て参照を事前に設計することと、同じ採点中に参照を動かすことを区別する。

### C5. 実現層を、音響誤差で軌道を修正する方式へ変える

既存Bankは**合法な初期解・予備tailの生成**に残す。全体を捨てない。しかし最終結果を数十本のランダム候補の選択だけで決めない。

軌道の自由パラメータθを、局所の転回値・時刻または少数の曲線係数とする。固定参照に対して

\[
J(\theta\mid\mathcal T_k)
=J_m(A_F(g_\theta);\mathcal T_k)+w_OJ_O+w_RJ_{rel}+w_sJ_{smooth}
\]

を実配合から計算し、合法集合内で小さくする。

**最小実装は有限差分を使う座標探索で十分。** 一時に少数パラメータを±へ動かし、全音響の同じJを評価して改善する変更だけ受け入れる。トラックを一つずつ試すことは、トラック別の独立目的関数を持つこととは違う。評価は常に共同配合。

```python
best = legal_warm_start
for sweep in range(max_refinement_sweeps):
    improved = False
    for parameter in chosen_local_parameters:
        options = propose_plus_minus(best, parameter, step)
        options = [x for x in options if hard_constraints_hold(x)]
        candidate = argmin([best] + options, score_against_frozen_reference)
        if joint_score(candidate) < joint_score(best) - improvement_epsilon:
            best = candidate
            improved = True
    if not improved:
        step *= 0.5
    if stop_condition_or_budget():
        break
return best
```

同じ目標に対する初期合法解を必ず候補に残すため、受理基準Jは悪化させない。複合目的ではtarget-fit単独が悪化する場合があるので、Jとfitを分けて示す。

解析勾配を使うなら、対称Gramに対して

\[
E_b=g^TQ_bg,\quad \nabla_gE_b=2Q_bg.
\]

さらに正規化・対数・寄与・関係・fluxを含む目的では、その合成微分が必要。「2Q_bg」を全mode損失の勾配としてそのまま使わない。微分実装が重いなら有限差分を選ぶ。

候補を修正するときは、`xi/parts/e_form/e_hist/e_motion` を再計算する。Diffusionの `_e_cache` は現在candidate IDをキーにするため、値を変更した候補に古いIDとキャッシュを使い回さない。新しいIDを付けるか、その候補のキャッシュを無効化する。改善前の合法解をin-place変更で失わない。

全ゲイン0では、エネルギー二次形式の一次勾配も0になる。開始・REOPENでは、合法な非ゼロ探索種を複数作ってから改善する。0からの勾配法だけで止まり、「可能性が開かない」としない。

今回の監査用人工例では、元のAnalyzerと合法Q5をそのまま使い、4つのピーク値を63回の小さい音響評価で修正すると、固定目標距離が0.502716→0.000325へ下がった。これは到達可能な一例に対する構成確認であり、実素材・四方式全体の性能保証ではない。**小さい実現側最適化が可能である根拠**として参照するだけでよい。

### C6. 形式の長さと確定区間を分離する【本改修の中心】

`cycle` を毎回数十秒まとめて選ぶことと、作品の大きい循環を同一にしない。

- `analysis_hop_seconds`：音響の観測解像度。既定0.1秒はそのままでもよい。
- `model_step_seconds`：方式の内部時間。解析hopと分離。
- `commit_seconds`：実現して履歴へ返す長さ。開始案1〜2秒。
- `lookahead_seconds`：音響誤差に向かって調整する未来範囲。開始案8〜12秒。
- `form`：OPEN、CONTRACT等の作品全体の大きい時間。

これらは試験開始案で、知覚上の普遍的閾値ではない。**1秒ごとにコミットすることは、1秒で0→100にすることでも、毎秒停止してQ5を開始することでもない。**

局所計画の開始状態は

\[
s_k=(n_k,g(n_k),\dot g(n_k),\ddot g(n_k),H_k).
\]

現在の `start_gains=0 or e0` 固定を使い回さない。現在の曲線の本当の微分を継承する。

実行手順：

```text
既に合法な残り軌道tailを持つ
→ 今回の短い未来窓に対する参照を準備・凍結
→ tailの局所部分を音響誤差に沿って有限回修正
→ 先頭commit_secondsだけ確定
→ その区間の実配合を履歴へ追加
→ 残りの未確定tailをwarm startとして次へ進む
```

全音声は最後にまとめてレンダリングしてよい。各窓で実オーディオデバイスへ出す必要はない。既知素材から確定ゲインで計算した実現配合を使用する。未採用の未来を「起きたこと」として履歴へ入れない。

新しい局所解が見つからなければ、**直前から保持している合法tailを続ける**。即時HOLDや無音、急速ゴール移行へ変更しない。新しい計画の全曲線を毎回無音／ゴールから始めない。

#### C6.1 Q5を毎区間停止させずに使うための最小拡張

既存Q5は真の停止端点・転回に残す。内部の接続には、一般の5次Hermite/Bernstein片を追加してよい。

区間長h、開始 \((g_a,v_a,a_a)\)、終了 \((g_b,v_b,a_b)\) に対する5次Bernstein係数は

\[
\begin{aligned}
b_0&=g_a,& b_1&=g_a+hv_a/5,& b_2&=g_a+2hv_a/5+h^2a_a/20,\\
b_5&=g_b,& b_4&=g_b-hv_b/5,& b_3&=g_b-2hv_b/5+h^2a_b/20.
\end{aligned}
\]

\(g(t)=\sum_{j=0}^5 b_j B_j^5(s)\)。これなら非ゼロ速度で内部節点を通過できる。終了状態の候補を変えるたび、合法な接続tailへ戻れることも確認する。

または、既存の合法な曲線へ、端で値・速度・加速度が0の局所補正

\[
\Delta g_i(t)=a_i\,64s^3(1-s)^3,\quad s\in[0,1]
\]

を追加する小さい方式でもよい。この補正は端点の状態を変えないため、既存のゴールtailを維持しやすい。ただし音量・微分の上限と本当の転回は必ず再検査する。旧Q5の検査式だけで、この6次の合成曲線も合格したことにしない。

**どちらか一方だけを選ぶ。二種類とも実装したり、汎用曲線フレームワークを作ったりしない。** 保守的なBernstein係数界または多項式の端点と導関数の実根を調べる小さい関数で十分。

低速区間・最小移動幅は、短い計画ブロックごとでなく、導関数の符号でつながる**実際の単調運動エピソード**に適用する。毎秒の小さい更新がΔmin未満だから不合格とすることも、全ブロックを転回例外と呼ぶことも禁止。

#### C6.2 ゴール到達可能性

各候補は、今の値・速度・加速度から、所定の時刻で `g=e0,v=a=0` になる合法なtailを持つ。直近窓だけの改善で将来の着地を壊さない。

C2を満たす基準tailの端点を保存した局所補正なら、補正区間以後の安全なtailを残せる。固定到達時刻に全く間に合わない設定は明示的失敗。形式がADAPTIVEの場合でも、ゴール露出方針を無断で破って前倒ししない。

### C7. 短い確定区間へ移るときの履歴更新

現在のEMA係数0.1を、そのまま毎秒適用すると、以前の67〜85秒ごとの0.1とは記憶時間が激変する。

\[
\rho(\Delta t)=1-\exp(-\Delta t/\tau_H)
\]

と、履歴の時定数を秒で設定する。\(\tau_H\) は作品用の設計値。最低でも短期・長期のどちらに何を保存するかを明示する。巨大な記憶管理は不要。

- 共通履歴：確定配合の平均・共変動・順序付きイベント。
- Diffusion：確定配合から次の場・方向を更新。
- VAE：確定配合から推定した潜在経路を更新。
- Transformer：確定イベントを次の注意対象に追加。
- GAN：確定配合経路と有力な代替案を区別し、親・識別器を維持。

未採用の候補は探索用アーカイブとして保存できるが、事実履歴と混ぜない。ゴール保持の定数列で運動履歴を上書きしない。

既存の「最近16イベント」は短い確定区間ではすぐ置き換わる。容量だけ増やすより、最近の短い列＋長期要約を小さく持つ。モード内の時間相関も \(\rho_{noise}=\exp(-\Delta t/\tau_{noise})\) として時間単位を明示する。

---

## D. 方式別に維持・変更するもの

### D1. Diffusion

維持：非ゼロの素材間結合、場エネルギー、確率的粒子、履歴依存方向、実配合への場評価。

変更：

1. 内側の実現改善中に、粒子を候補音へ50%引き寄せない。場・粒子の参照スナップショットを保存。
2. 複数の候補目標を準備する段階のLangevin的探索は残す。内部反復と音楽時間は別として記録。
3. anchorとペア・三者項を別表示。値の割合だけで重みを自動的に逆転しない。固定目標に対する候補の改善と、場の候補間差を確認する。
4. 次の確定区間では、実際に採用した配合から結合・方向を更新してよい。ここが正当な履歴付き再可能化。
5. 目標列の存在しない帯域寄与・負の寄与等は、到達不能残差として認める。ゲイン制約を破って実現しない。

### D2. VAE型の共有潜在配合

維持：少数の共有潜在軸、非退化な分布、ゴールへの潜在収縮、確定経路に依存する次の分布。大規模VAE学習は不要。

変更：

1. ファイル順round-robinによる固定グループを、音響から作る共有因子へ置き換える。
2. 少数の合法なprobe配合または候補配合の \(S_A(A-A^G)\) を列とする小行列のSVDからk方向を得る。kは小さいまま（通常2、必要なら3、k<M）。
3. デコーダは \(A^*=A^G+B\tanh z\) 等の明示した共同写像とする。ゲインのデコーダではない。
4. 音源順変更で配合意味が無意識に変わらないよう、素材IDとSVDの符号規則を保存。基底の安定化を巨大な実装にしない。
5. 同一基底の区間内で潜在履歴を使う。基底を変更するなら過去の実配合を新基底に再投影するか、基底を大きいcycle内で固定する。
6. 実現改善中のz_shiftによる目標追従を止める。次の確定区間の履歴更新は残す。

単に「kを大きくする」「乱数を強くする」だけでは完了としない。

### D3. Transformer型の関係・次配合

維持：現在素材と確定イベントの注意、複数の参照関係、理想系列の条件付き生成。attentionをゲインへ直接代入しない。

変更：

1. 次の短い確定区間の参照を作るとき、直前に実現した音響を入力する。unit末だけの更新から移る。
2. `xi_start` を単位開始の無音／ゴールから毎回再構成せず、実現済み現在から開始する。
3. 仮説は候補内コンテキスト、確定結果は共有履歴という区別を守る。
4. 既存のdt補正は維持。`analysis_hop` と `model_step`、AR相関時間を分離する。有界化も同じモデルステップで行う。
5. 放射状tanhは残してよい。半径を大きくするだけ、微細ノイズを増やすだけでは修正にならない。
6. 参照が変わったときに固定目標も変わり、実現層がその差へ合法に動けることを一つの非退化例で確認する。

### D4. GAN型の敵対的配合と系譜

維持：識別器と生成側の交互更新、実音響・交互作用を含む評価、親＋新規探索、補正済みの標本再利用を続ける場合のESS確認。

変更：

1. **基底を変更したときの生成座標の無変換コピーを止める。** 最小案は、親音響・親ID・critic・共通履歴を維持し、新基底の変位平均を0へ再初期化する。何を継承し何を再初期化したかを記録。
2. 旧変位も保存する案なら、同じ相対時間座標へ整列した基底について
   \[
   T=B_{new}^{+}\mathcal I B_{old},\quad\mu_{new}=T\mu_{old},\quad C_{new}=TC_{old}T^T
   \]
   とする。\(\mathcal I\) は定義済みの時間座標への写像。対角分散だけ残すなら非対角情報を捨てた近似を明記。今回そこまで必要なければ案1を選ぶ。
3. D/Gは同じ参照・同じ候補集合を固定した更新単位の中で交互に動かし、実現候補探索との切替を明示。旧標本・動くBank・動く基底の意味を混ぜない。
4. 既存Gaussian方式の整合維持が重い場合は、v1.1にあった有限候補上の \(p_j=softmax(\theta_j)\) とロジスティック識別器へ限定的に簡略化してよい。両側更新は残す。これは名前だけのGANへ置換する意味ではない。
5. 固定D・固定候補上の \(\ell_j=-\log D(A_j)+\lambda J_{common,j}\) に対し
   \[
   \partial_{\theta_i}\sum_jp_j\ell_j=p_i(\ell_i-\bar\ell)
   \]
   を使えば、小さい明示的な更新にできる。親事前KLを使うならその勾配も追加する。
6. 参照生成に同じ曲線族を使うことは禁止しない。ただし出力側実現層を改善した後の候補を含める。現状の狭いBankだけで「豊かな分布を学習した」と説明しない。
7. 親確率を必ず偏らせる、必ず過去親を採用する、必ずmode collapseさせる、という合格条件は作らない。

---

## E. 時間設定・速度プロファイル・音響の評価

### E1. 長さは、比較用と作曲用を分ける

既存 `project.json` は旧結果再現用として残し、別の `project.explore.json` を作る。固定形式は引き続き許可する。

今回の素材の周回を含めて確認する**試験案**：

```json
{
  "cycles": 2,
  "intro_seconds": 12,
  "open_seconds": 320,
  "contract_seconds": 30,
  "goal_hold_seconds": 5,
  "reopen_seconds": 30,
  "final_reopen": true
}
```

この形式の合計は782秒。最初のゴール362秒、次747秒、最後に再可能化を含む。これは音楽的な最適長ではなく、現在の最長約709.5秒の素材も一度周回する検討用条件。

設定値は例であり利用者が変えられる。厳密固定の時間が運動と衝突する場合、勝手な高速化はしない。長尺版を24移動の同じ全体Bankへ無理に押し込むだけの対応は禁止。

### E2. 「順次的」と「遅い」を同一視しない

最初は既存プロファイルのまま、局所再計画・非停止接続・中小振幅の選択を改善する。

その後の試聴比較が必要なら、明示した別プロファイル `responsive_x2_UNVERIFIED` を追加してよい。単に旧上限を無視するのではなく、k=2の時間短縮に対応して

\[
V'_{max}=kV_{max},\quad A'_{max}=k^2A_{max},\quad J'_{max}=k^3J_{max},\quad L'_{max}=kL_{max}
\]

とする。最低速度もk倍、許容ramp時間は1/k、振幅・低速割合は不変にすれば、同じ曲線の時間短縮との関係が明確になる。元値0.12/0.08/0.12なら0.24/0.32/0.96。これは**聴覚上承認済みの値ではない**。

別プロファイルのWAVを旧条件と混ぜない。最終採用の自然さは利用者の試聴で判断する。速度が違うだけで「モデル差が出た」と評価しない。

### E3. 無音・単独前景への退化はSOFTで扱う

現在の実配合から、非ゴール寄与を再正規化した \(p_i\) を作り、

\[
N_{eff}=\frac1{\sum_{i>0}p_i^2}
\]

を補助量にできる。OPENに限り \([max(0,N_{target}-N_{eff})]^2\) を弱く加える。初期案 \(N_{target}=2\) は試験用であり、常時2素材を強制するHARD条件ではない。ゼロエネルギーでは別の無音フラグを使う。

実混合のエネルギー不足も独立に計算する。単一トラックの相対寄与が1だから豊かなOPENと判断しない。ただし意図した疎な部分・素材自身の無音まで加工して埋めない。

初回の全素材ランダムZERO_HOLD重複はデフォルトで避ける。素材が鳴っている候補のうち複数を、同じ開始時計のまま合法に立ち上げる。開始音が元素材の無音で存在しない場合は、その限界を記録する。

### E4. 近似品質の記録を、全体へ偏りなく置く

旧 `rendered_checks()` の中央47窓だけを、全長の品質保証と呼ばない。重い全候補の再FFTは不要。

- 全解析gridで代理評価に対する固定目標残差を計算。
- 実PCMの再解析は、各大段階へ等間隔に配置した少数窓とゴール境界を含める。
- fluxは隣接する一つ前の窓も読む。離れた評価窓同士の差を0.1秒fluxとして扱わない。
- `proxy_vs_rendered`, `fixed_target_vs_proxy`, `fixed_target_vs_rendered` を分ける。
- 目標列は固定評価に使ったものを保存する。平均だけでなく、同じ解析時刻へ復元できる表現を残す。添付ファイル数を増やしたくなければtrace内でもよい。
- この距離を「一致率」「人間が聴き分けられる確率」へ変換しない。

---

## F. 最小の確認と完了条件

検証基盤を作らず、既存スモークと修正実行に次の確認をまとめる。**以下の確認は改修の根拠であり、全ケースの完全証明を求めるものではない。**

1. **結合の修正**：B3のsync例が合法に作れる。counterは内点区間で両者の変化へ作用する。0開始で反対方向が不可能なのは正しく扱う。
2. **固定目標への実現改善**：到達可能な非退化の小さい目標で、目標hashを変えずにJが下がる。理想をAへ寄せた値を改善率へ入れない。一般の実素材に誤差ゼロを要求しない。
3. **局所確定と境界**：少なくとも一つの内部接続点で非ゼロ速度を継ぎ、値・速度・加速度が連続。確定した過去は変わらず、指定ゴールへ合法に着地する。
4. **4方式と履歴**：4種類の参照機構が実行される。同じ現在・音声・乱数で、履歴を変えると内部参照が変わる非退化例を一つ確認し、実現音への差は別に記録。差を強制するための後付けノイズは禁止。
5. **音声と成果物**：音量のみの再合成、固定ループ、ゴール保持、実設定とcurve保存。短いfixtureで4方式を確認し、長尺実素材版はその後に生成。未提供素材や未校正の自然さをPASSと書かない。

数日間の網羅テスト、全seed、全N、全OS、完全性のための事前実装は不要。解析スクリプトを一式再現することを作業開始の前提にしない。

### 完了報告に必須の表

```text
変更箇所 / 維持した箇所 / 未実装・未確認
各mode:
  固定目標での初期J → 最終J（参照hash不変）
  target-fit / 場等の追加項 / 実PCM残差
  commit回数と、履歴を実際に更新した回数
  非ゼロ速度での接続数・ハード違反数
  goal上昇開始・完全goalの時刻
  音量移動の振幅と所要時間の分布
  内部履歴効果 / 実現音への効果（同一条件比較の範囲を明記）
  元素材のループ回数
```

`BEST_EFFORT` / `OK_APPROXIMATE` は正当な出力。数値残差が残ることと、機構を削除したことを区別する。

**核心の基準：理想を動かしただけでなく、実配合を近づける計算が働き、その採用結果が次の可能性へ返っていること。** これが未実装なら、長尺のWAVが4本出ても、今回の改修を完了としない。

---

## G. そのまま渡す短い実行指示

> 添付の「コード002再監査・Claude Code修正指示【統合版】」を今回の修正指示とする。元の音量のみ・固定ループ・滑らかさ・厳密ゴール・4原理・履歴は維持する。古い `docs/MODE_CONTRACT.md` の共有ファイル編集禁止と、候補へ理想を引き寄せる前提は撤回する。
>
> まずsyncの終点不等号とcounterの全区間境界矛盾を直す。次に、理想配合を固定した実現側の音響誤差駆動改善を実装する。長い作品形式の内側で短い区間を確定し、非ゼロ速度・加速度を継承して、採用配合を次の履歴へ返す。目標を動かした残差低下を、音の改善と報告しない。
>
> Gram解析とレンダラーは保つ。VAEの素材番号依存基底、GANの基底変更時の座標継承、OPENの退化と時間設定は本文の順で修正する。4方式を大型モデルへ置き換えず、GUI・インフラ・網羅検証を作らない。監査用環境の再構築も不要。許可された実装枠を超える完全性追求はせず、実働部分と未達を明示する。

---

## H. 今回の証拠と、主張していないこと

追加の限定的な点検：

- ZIP主要25ファイルと過去コミットのblob照合：25/25一致。
- 実関数による、合法同期曲線の不当拒否：5/5再現。
- 監査用メモリ内の不等号修正例：同じ5条件で5/5合法。
- 同じ開始境界でのcounter拒否：実関数で確認。
- OPENの単独素材／複数素材に対する共通ペナルティ：両方0。
- GANの生成座標の基底符号反転に対する無変換継承：構成例で確認。
- `velocity_after` が方向を返すこと：実関数で確認。現コア呼び出しなし。
- 固定音響目標に対する小さい実現側座標探索：元のAnalyzer、人工信号、63評価、合法曲線のまま距離0.502716→0.000325。

**主張していないこと**：全出力を今回再生成した、原素材を再解析した、新しい実装が完成した、実素材で4方式の違いが必ず聴き取れる、任意の潜在音響をゲインだけで作れる、上記の追加設計が5時間で必ず終わる。

本指示は前回の分析を補強・限定する統合版である。正しい既存部分を維持しつつ、内部状態が音に届かない原因を、コードと数式の両方で直接修正することを求める。
